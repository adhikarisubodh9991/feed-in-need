# Frontend Environment Variables

# API URL is no longer needed - using Vite proxy in dev, same domain in production
# VITE_API_URL=/api (relative paths used automatically)

# Google OAuth Client ID
# Get this from Google Cloud Console: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
